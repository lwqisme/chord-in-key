# Example .npmrc file
# Uncomment and modify the settings you need

# Set the default registry
# registry=https://registry.npmjs.org/

# Configure proxy if you're behind a corporate firewall
# proxy=http://proxy.example.com:8080
# https-proxy=http://proxy.example.com:8080

# Set default save behavior
save=true
save-exact=false

# Set log level (silent, error, warn, notice, http, timing, info, verbose, silly)
loglevel=notice

# Set timeout (in ms)
# timeout=60000

# Set cache location and settings
# cache=~/.npm
# cache-min=10

# Set package access level for publishing
# access=public
